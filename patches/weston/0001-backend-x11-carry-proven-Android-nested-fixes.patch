From 2c71340f4f662b4325e022cefd8061ad98f1adf5 Mon Sep 17 00:00:00 2001
From: BlackFireAlex <8484750+BlackFireAlex@users.noreply.github.com>
Date: Sat, 29 Aug 2026 19:31:57 -0700
Subject: [PATCH] backend-x11: carry proven Android nested fixes

---
 clients/desktop-shell.c           |  4 ++
 clients/meson.build               |  2 +-
 clients/touch-calibrator.c        |  7 +++
 frontend/main.c                   | 19 +++++---
 frontend/meson.build              |  1 -
 libweston/backend-vnc/meson.build |  2 +-
 libweston/backend-x11/meson.build |  6 +++
 libweston/backend-x11/x11.c       | 72 ++++++++++++++++++++++++++++++-
 libweston/input.c                 |  5 +++
 libweston/libweston-internal.h    |  2 +
 libweston/meson.build             |  2 +-
 meson.build                       |  1 -
 shared/xalloc.h                   |  4 ++
 xwayland/launcher.c               | 10 ++---
 14 files changed, 120 insertions(+), 17 deletions(-)

diff --git a/clients/desktop-shell.c b/clients/desktop-shell.c
index e06613ff..88b826e6 100644
--- a/clients/desktop-shell.c
+++ b/clients/desktop-shell.c
@@ -1176,6 +1176,10 @@ background_create(struct desktop *desktop, struct output *output)
 	struct weston_config_section *s;
 	char *type;
 
+#ifdef __ANDROID__
+	const char *program_invocation_short_name = getprogname();
+#endif
+
 	background = xzalloc(sizeof *background);
 	background->owner = output;
 	background->base.configure = background_configure;
diff --git a/clients/meson.build b/clients/meson.build
index 44a84b9d..7b7d1a8d 100644
--- a/clients/meson.build
+++ b/clients/meson.build
@@ -77,7 +77,7 @@ simple_clients = [
 			dep_libshared,
 			dep_pixman,
 			dep_libdrm,
-			dependency('libudev', version: '>= 136'),
+           dependency('libudev', version: '>= 136', required: simple_build_all or simple_clients_enabled.contains('dmabuf-feedback')),
 			# gbm_bo_get_fd_for_plane() from 21.1.0
 			dependency('gbm', version: '>= 21.1.1',
 				required: simple_build_all or simple_clients_enabled.contains('dmabuf-feedback'),
diff --git a/clients/touch-calibrator.c b/clients/touch-calibrator.c
index 49dd9206..69f1f784 100644
--- a/clients/touch-calibrator.c
+++ b/clients/touch-calibrator.c
@@ -870,6 +870,10 @@ pr_err(const char *fmt, ...)
 {
 	va_list argp;
 
+#ifdef __ANDROID__
+	const char *program_invocation_short_name = getprogname();
+#endif
+
 	va_start(argp, fmt);
 	fprintf(stderr, "%s error: ", program_invocation_short_name);
 	vfprintf(stderr, fmt, argp);
@@ -879,6 +883,9 @@ pr_err(const char *fmt, ...)
 static void
 help(void)
 {
+#ifdef __ANDROID__
+	const char *program_invocation_short_name = getprogname();
+#endif
 	fprintf(stderr, "Compute a touchscreen calibration matrix for "
 		"a Wayland compositor by\n"
 		"having the user touch points on the screen.\n\n");
diff --git a/frontend/main.c b/frontend/main.c
index d135e77d..7e90ef79 100644
--- a/frontend/main.c
+++ b/frontend/main.c
@@ -41,7 +41,9 @@
 #include <sys/stat.h>
 #include <sys/wait.h>
 #include <sys/socket.h>
+#ifndef __ANDROID__
 #include <libinput.h>
+#endif
 #include <libevdev/libevdev.h>
 #include <linux/input.h>
 #include <sys/time.h>
@@ -1070,7 +1072,7 @@ load_additional_modules(struct wet_compositor wet)
 		load_pipewire(wet.compositor, wet.config);
 	}
 }
-
+#ifndef __ANDROID__
 static int
 save_touch_device_calibration(struct weston_compositor *compositor,
 			      struct weston_touch_device *device,
@@ -1128,7 +1130,7 @@ out:
 
 	return ret;
 }
-
+#endif
 static const struct {
 	const char *name;
 	enum require_outputs mode;
@@ -1208,14 +1210,14 @@ weston_compositor_init_config(struct weston_compositor *ec,
 		else
 			compositor->use_color_manager = true;
 	}
-
+#ifndef __ANDROID__
 	/* weston.ini [libinput] */
 	s = weston_config_get_section(config, "libinput", NULL, NULL);
 	weston_config_section_get_bool(s, "touchscreen_calibrator", &cal, 0);
 	if (cal)
 		weston_compositor_enable_touch_calibrator(ec,
 						save_touch_device_calibration);
-
+#endif
 	return 0;
 }
 
@@ -2127,6 +2129,7 @@ simple_heads_changed(struct wl_listener *listener, void *arg)
 	}
 }
 
+#ifndef __ANDROID__
 static void
 configure_input_device_accel(struct weston_config_section *s,
 		struct libinput_device *device)
@@ -2328,7 +2331,7 @@ configure_input_device(struct weston_compositor *compositor,
 
 	configure_input_device_scroll(s, device);
 }
-
+#endif
 static int
 drm_backend_output_configure(struct weston_output *output,
 			     struct weston_config_section *section)
@@ -3375,6 +3378,7 @@ static int
 load_drm_backend(struct weston_compositor *c, int *argc, char **argv,
 		 struct weston_config *wc, enum weston_renderer_type renderer)
 {
+#ifndef __ANDROID__
 	struct weston_drm_backend_config config = {{ 0, }};
 	struct weston_config_section *section;
 	struct wet_compositor *wet = to_wet_compositor(c);
@@ -3438,7 +3442,10 @@ load_drm_backend(struct weston_compositor *c, int *argc, char **argv,
 	free(config.gbm_format);
 	free(config.seat_id);
 	free(config.specific_device);
-
+#else
+	weston_log("error: drm_backend is not avaiable on Android.\n");
+	abort();
+#endif
 	return 0;
 }
 
diff --git a/frontend/meson.build b/frontend/meson.build
index ddaf6771..f7c1a2f6 100644
--- a/frontend/meson.build
+++ b/frontend/meson.build
@@ -12,7 +12,6 @@ srcs_weston = [
 deps_weston = [
 	dep_libshared,
 	dep_libweston_public,
-	dep_libinput,
 	dep_libevdev,
 	dep_libdl,
 	dep_threads,
diff --git a/libweston/backend-vnc/meson.build b/libweston/backend-vnc/meson.build
index cf67c51c..bb82fba7 100644
--- a/libweston/backend-vnc/meson.build
+++ b/libweston/backend-vnc/meson.build
@@ -8,7 +8,7 @@ if not dep_neatvnc.found()
 	error('VNC backend requires neatvnc which was not found. Or, you can use \'-Dbackend-vnc=false\'.')
 endif
 
-dep_aml = dependency('aml', version: ['>= 0.3.0', '< 0.4.0'], required: false, fallback: ['aml', 'aml_dep'])
+dep_aml = dependency('aml1', version: ['>= 1.0.0', '< 2.0.0'], required: false)
 if not dep_aml.found()
 	error('VNC backend requires libaml which was not found. Or, you can use \'-Dbackend-vnc=false\'.')
 endif
diff --git a/libweston/backend-x11/meson.build b/libweston/backend-x11/meson.build
index 5d30bcce..6f027091 100644
--- a/libweston/backend-x11/meson.build
+++ b/libweston/backend-x11/meson.build
@@ -32,6 +32,12 @@ foreach name : [ 'xcb-shm', 'x11', 'x11-xcb' ]
 	deps_x11 += d
 endforeach
 
+dep_xcb_randr = dependency('xcb-randr', required: false)
+if not dep_xcb_randr.found()
+	error('x11-backend requires xcb-randr which was not found. Or, you can use \'-Dbackend-x11=false\'.')
+endif
+deps_x11 += dep_xcb_randr
+
 dep_xcb_xkb = dependency('xcb-xkb', version: '>= 1.9', required: false)
 if dep_xcb_xkb.found()
 	deps_x11 += dep_xcb_xkb
diff --git a/libweston/backend-x11/x11.c b/libweston/backend-x11/x11.c
index 8c87d9ed..5c052114 100644
--- a/libweston/backend-x11/x11.c
+++ b/libweston/backend-x11/x11.c
@@ -40,6 +40,7 @@
 #include <linux/input.h>
 
 #include <xcb/xcb.h>
+#include <xcb/randr.h>
 #include <xcb/shm.h>
 #ifdef HAVE_XCB_XKB
 #include <xcb/xkb.h>
@@ -196,6 +197,73 @@ x11_compositor_get_default_screen(struct x11_backend *b)
 	return xcb_setup_roots_iterator(xcb_get_setup(b->conn)).data;
 }
 
+/* The stock X11 backend advertises a fixed 60 Hz mode even when its parent X
+ * server runs faster. Termux:X11 publishes Android's current display cadence
+ * through XRandR, so use the active CRTC's real timing for nested frame pacing.
+ * Fall back to Weston's historical 60 Hz value if RandR is unavailable. */
+static int32_t
+x11_compositor_get_refresh_mhz(struct x11_backend *b)
+{
+	xcb_randr_get_screen_resources_current_reply_t *resources;
+	xcb_randr_crtc_t *crtcs;
+	xcb_randr_mode_info_t *modes;
+	int crtc_count, mode_count, i, j;
+	int32_t refresh = 60000;
+	const char *override;
+	char *override_end;
+	long override_value;
+
+	override = getenv("WESTON_X11_REFRESH_MHZ");
+	if (override) {
+		errno = 0;
+		override_value = strtol(override, &override_end, 10);
+		if (!errno && override_end != override && *override_end == '\0' &&
+		    override_value >= 30000 && override_value <= 240000)
+			return (int32_t)override_value;
+	}
+
+	resources = xcb_randr_get_screen_resources_current_reply(
+		b->conn,
+		xcb_randr_get_screen_resources_current(b->conn, b->screen->root),
+		NULL);
+	if (!resources)
+		return refresh;
+
+	crtcs = xcb_randr_get_screen_resources_current_crtcs(resources);
+	crtc_count = xcb_randr_get_screen_resources_current_crtcs_length(resources);
+	modes = xcb_randr_get_screen_resources_current_modes(resources);
+	mode_count = xcb_randr_get_screen_resources_current_modes_length(resources);
+
+	for (i = 0; i < crtc_count; ++i) {
+		xcb_randr_get_crtc_info_reply_t *crtc;
+
+		crtc = xcb_randr_get_crtc_info_reply(
+			b->conn,
+			xcb_randr_get_crtc_info(b->conn, crtcs[i], resources->config_timestamp),
+			NULL);
+		if (!crtc)
+			continue;
+
+		for (j = 0; j < mode_count; ++j) {
+			uint64_t denominator;
+
+			if (modes[j].id != crtc->mode)
+				continue;
+			denominator = (uint64_t)modes[j].htotal * modes[j].vtotal;
+			if (denominator)
+				refresh = (int32_t)(((uint64_t)modes[j].dot_clock * 1000 + denominator / 2) / denominator);
+			break;
+		}
+
+		free(crtc);
+		if (refresh != 60000)
+			break;
+	}
+
+	free(resources);
+	return refresh;
+}
+
 static struct xkb_keymap *
 x11_backend_get_keymap(struct x11_backend *b)
 {
@@ -1176,7 +1244,9 @@ x11_output_set_size(struct weston_output *base, int width, int height)
 
 	output->mode.width = output_width;
 	output->mode.height = output_height;
-	output->mode.refresh = 60000;
+	output->mode.refresh = x11_compositor_get_refresh_mhz(b);
+	weston_log("x11 output refresh selected: %.3f Hz\n",
+		   output->mode.refresh / 1000.0);
 	output->native = output->mode;
 	output->scale = output->base.current_scale;
 	wl_list_insert(&output->base.mode_list, &output->mode.link);
diff --git a/libweston/input.c b/libweston/input.c
index 0aee74d2..f8b2f69f 100644
--- a/libweston/input.c
+++ b/libweston/input.c
@@ -34,7 +34,12 @@
 #include <sys/mman.h>
 #include <assert.h>
 #include <unistd.h>
+#ifndef __ANDROID__
 #include <values.h>
+#else
+#include <limits.h>
+#include <float.h>
+#endif
 #include <fcntl.h>
 #include <limits.h>
 #include <errno.h>
diff --git a/libweston/libweston-internal.h b/libweston/libweston-internal.h
index 0cdb9e2d..5c4e6602 100644
--- a/libweston/libweston-internal.h
+++ b/libweston/libweston-internal.h
@@ -142,6 +142,7 @@ struct weston_renderer {
 	void (*remove_renderbuffer_dmabuf)(struct weston_output *output,
 					   struct weston_renderbuffer *renderbuffer);
 
+#ifndef __ANDROID__
 	/* Allocate a DMABUF that can be imported as renderbuffer
 	 *
 	 * \param renderer The renderer that allocated the DMABUF
@@ -161,6 +162,7 @@ struct weston_renderer {
 					unsigned int width, unsigned int height,
 					uint32_t format,
 					const uint64_t *modifiers, unsigned int count);
+#endif
 
 	enum weston_renderer_type type;
 	const struct gl_renderer_interface *gl;
diff --git a/libweston/meson.build b/libweston/meson.build
index d1f126f2..4a3e52f1 100644
--- a/libweston/meson.build
+++ b/libweston/meson.build
@@ -180,7 +180,6 @@ if get_option('backend-drm')
 		install: false
 	)
 	dep_session_helper = declare_dependency(link_with: lib_session_helper)
-endif
 
 lib_libinput_backend = static_library(
 	'libinput-backend',
@@ -201,6 +200,7 @@ dep_libinput_backend = declare_dependency(
 	link_with: lib_libinput_backend,
 	include_directories: include_directories('.')
 )
+endif
 
 dep_vertex_clipping = declare_dependency(
 	sources: 'vertex-clipping.c',
diff --git a/meson.build b/meson.build
index fd14030c..6b183fa5 100644
--- a/meson.build
+++ b/meson.build
@@ -147,7 +147,6 @@ endif
 dep_wayland_server = dependency('wayland-server', version: '>= 1.22.0')
 dep_wayland_client = dependency('wayland-client', version: '>= 1.22.0')
 dep_pixman = dependency('pixman-1', version: '>= 0.25.2')
-dep_libinput = dependency('libinput', version: '>= 1.2.0')
 dep_libevdev = dependency('libevdev')
 dep_libm = cc.find_library('m')
 dep_libdl = cc.find_library('dl')
diff --git a/shared/xalloc.h b/shared/xalloc.h
index a2747f9c..2835b343 100644
--- a/shared/xalloc.h
+++ b/shared/xalloc.h
@@ -42,6 +42,10 @@ abort_oom_if_null(void *p)
 	static const char oommsg[] = ": out of memory\n";
 	size_t written __attribute__((unused));
 
+#ifdef __ANDROID__
+	const char *program_invocation_short_name = getprogname();
+#endif
+
 	if (p)
 		return p;
 
diff --git a/xwayland/launcher.c b/xwayland/launcher.c
index dd4ecdc8..ea802b47 100644
--- a/xwayland/launcher.c
+++ b/xwayland/launcher.c
@@ -78,9 +78,9 @@ weston_xserver_shutdown(struct weston_xserver *wxs)
 {
 	char path[256];
 
-	snprintf(path, sizeof path, "/tmp/.X%d-lock", wxs->display);
+	snprintf(path, sizeof path, "@TERMUX_PREFIX@/tmp/.X%d-lock", wxs->display);
 	unlink(path);
-	snprintf(path, sizeof path, "/tmp/.X11-unix/X%d", wxs->display);
+	snprintf(path, sizeof path, "@TERMUX_PREFIX@/tmp/.X11-unix/X%d", wxs->display);
 	unlink(path);
 	if (wxs->client) {
 		wl_client_destroy(wxs->client);
@@ -111,7 +111,7 @@ bind_to_abstract_socket(int display)
 
 	addr.sun_family = AF_LOCAL;
 	name_size = snprintf(addr.sun_path, sizeof addr.sun_path,
-			     "%c/tmp/.X11-unix/X%d", 0, display);
+			     "%c@TERMUX_PREFIX@/tmp/.X11-unix/X%d", 0, display);
 	size = offsetof(struct sockaddr_un, sun_path) + name_size;
 	if (bind(fd, (struct sockaddr *) &addr, size) < 0) {
 		weston_log("failed to bind to @%s: %s\n", addr.sun_path + 1,
@@ -141,7 +141,7 @@ bind_to_unix_socket(int display)
 
 	addr.sun_family = AF_LOCAL;
 	name_size = snprintf(addr.sun_path, sizeof addr.sun_path,
-			     "/tmp/.X11-unix/X%d", display) + 1;
+			     "@TERMUX_PREFIX@/tmp/.X11-unix/X%d", display) + 1;
 	size = offsetof(struct sockaddr_un, sun_path) + name_size;
 	unlink(addr.sun_path);
 	if (bind(fd, (struct sockaddr *) &addr, size) < 0) {
@@ -169,7 +169,7 @@ create_lockfile(int display, char *lockfile, size_t lsize)
 	int fd, size;
 	pid_t other;
 
-	snprintf(lockfile, lsize, "/tmp/.X%d-lock", display);
+	snprintf(lockfile, lsize, "@TERMUX_PREFIX@/tmp/.X%d-lock", display);
 	fd = open(lockfile, O_WRONLY | O_CLOEXEC | O_CREAT | O_EXCL, 0444);
 	if (fd < 0 && errno == EEXIST) {
 		fd = open(lockfile, O_CLOEXEC | O_RDONLY);
